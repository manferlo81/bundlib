version: 2.1

orbs:
  codecov: codecov/codecov@4.1.0

executors:
  executor_node:
    docker:
      - image: cimg/node:20.15.1
    working_directory: ~/bundlib

commands:
  command_checkout_cache_install:
    steps:
      - checkout
      - restore_cache:
          keys:
            - v8-deps-bundlib-{{ checksum "package.json" }}
            - v8-deps-bundlib-
      - run:
          name: Install dependencies
          command: npm install
      - save_cache:
          paths:
            - node_modules
          key: v8-deps-bundlib-{{ checksum "package.json" }}

  command_test_and_build:
    steps:
      - run:
          name: Test
          command: npm test
      - run:
          name: Build
          command: npm run build

  command_save_workspace:
    steps:
      - persist_to_workspace:
          root: ~/bundlib
          paths:
            - .

  command_restore_workspace:
    steps:
      - attach_workspace:
          at: ~/bundlib

  command_publish_to_npm:
    steps:
      - run:
          name: Login to npm
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > .npmrc
      - run:
          name: Publish to npm
          command: npm publish

jobs:
  job_test_and_build:
    executor: executor_node
    steps:
      - command_checkout_cache_install
      - command_test_and_build
      - command_save_workspace

  job_upload_coverage:
    executor: executor_node
    steps:
      - command_restore_workspace
      - codecov/upload:
          file: coverage/*.json

  job_publish_to_npm:
    executor: executor_node
    steps:
      - command_restore_workspace
      - command_publish_to_npm

workflows:
  workflow_ci:
    jobs:
      - job_test_and_build:
          filters:
            tags:
              only: /.*/
            branches:
              only: /.*/

      - job_upload_coverage:
          requires:
            - job_test_and_build
          filters:
            branches:
              only: /master/

      - job_publish_to_npm:
          context: npm
          requires:
            - job_test_and_build
          filters:
            tags:
              only: /^v\d+\.\d+\.\d+$/
            branches:
              ignore: /.*/
